# Samples

Do not commit personal exported settings by default.

Future regression fixtures should be either:

- generated from public upstream defaults; or
- purpose-built synthetic fixtures containing no user-specific device IDs or local paths.
